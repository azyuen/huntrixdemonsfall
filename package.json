{
  "name": "huntrixdemonsfall",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "validate:rumi": "node scripts/validate-rumi-atlas.mjs",
    "build": "npm run validate:rumi && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
